version: "3"

services:
  wppconnect:
    container_name: wpp-server
    restart: unless-stopped
    build:
      context: .
    environment:
      - VIRTUAL_HOST=api.masksan.com
      - VIRTUAL_PORT=21465
      - LETSENCRYPT_HOST=api.masksan.com
      - LETSENCRYPT_EMAIL=hmoro.dev@gmail.com
    volumes:
      - ./config.ts:/usr/src/wpp-server/config.ts
      - ./wppconnect_tokens:/usr/src/wpp-server/tokens
      - ./userDataDir:/usr/src/wpp-server/userDataDir
    entrypoint: sh -c "find /usr/src/wpp-server/userDataDir -name 'SingletonLock' -delete 2>/dev/null; node dist/server.js"
    ports:
      - "21465:21465"
    networks:
      - nginx-public
      - default

volumes:
  wppconnect_tokens: {}

networks:
  nginx-public:
    external: true